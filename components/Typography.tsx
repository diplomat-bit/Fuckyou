import React from "react";
export const Typography = ({ children }: { children: React.ReactNode }) => <>{children}</>;
export default Typography;

// --- CONSOLIDATED FROM: Typography_1.tsx ---

import React from "react";
export const Typography = ({ children }: { children: React.ReactNode }) => <>{children}</>;
export default Typography;