import React from 'react';

export const NexusLink: React.FC<{ to: string; children: React.ReactNode }> = ({ to, children }) => (
  <span className="text-blue-400 hover:text-blue-300 cursor-pointer underline">
    {children}
  </span>
);

// --- CONSOLIDATED FROM: ./shared/NexusLink_2.tsx ---


import React from 'react';

export const NexusLink: React.FC<{ to: string; children: React.ReactNode }> = ({ to, children }) => (
  <span className="text-blue-400 hover:text-blue-300 cursor-pointer underline">
    {children}
  </span>
);


// --- CONSOLIDATED FROM: ./shared/NexusLink_1.tsx ---

import React from 'react';

export const NexusLink: React.FC<{ to: string; children: React.ReactNode }> = ({ to, children }) => (
  <span className="text-blue-400 hover:text-blue-300 cursor-pointer underline">
    {children}
  </span>
);

// --- CONSOLIDATED FROM: ./shared/NexusLink.tsx ---


import React from 'react';

export const NexusLink: React.FC<{ to: string; children: React.ReactNode }> = ({ to, children }) => (
  <span className="text-blue-400 hover:text-blue-300 cursor-pointer underline">
    {children}
  </span>
);


// --- CONSOLIDATED FROM: ./components/shared (1)/NexusLink.tsx ---

import React from 'react';

export const NexusLink: React.FC<{ to: string; children: React.ReactNode }> = ({ to, children }) => (
  <span className="text-blue-400 hover:text-blue-300 cursor-pointer underline">
    {children}
  </span>
);

// --- CONSOLIDATED FROM: ./components/shared/NexusLink.tsx ---


import React from 'react';

export const NexusLink: React.FC<{ to: string; children: React.ReactNode }> = ({ to, children }) => (
  <span className="text-blue-400 hover:text-blue-300 cursor-pointer underline">
    {children}
  </span>
);


// --- CONSOLIDATED FROM: ./components/shared (2)/NexusLink.tsx ---


import React from 'react';

export const NexusLink: React.FC<{ to: string; children: React.ReactNode }> = ({ to, children }) => (
  <span className="text-blue-400 hover:text-blue-300 cursor-pointer underline">
    {children}
  </span>
);
