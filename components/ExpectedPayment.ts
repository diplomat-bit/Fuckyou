

// --- CONSOLIDATED FROM: ./types/ExpectedPayment.ts ---


export interface ExpectedPayment {
    id: string;
    amount_upper_bound: number;
    direction: string;
    type: string;
    date_lower_bound: string;
    description: string;
}
      

// --- CONSOLIDATED FROM: ./ExpectedPayment.ts ---


export interface ExpectedPayment {
    id: string;
    amount_upper_bound: number;
    direction: string;
    type: string;
    date_lower_bound: string;
    description: string;
}
      