import React from 'react';

export const NexusResourceJSON: React.FC<{ resource: object }> = ({ resource }) => {
  return (
    <div className="bg-gray-900 text-gray-100 p-4 rounded-md font-mono text-xs overflow-x-auto border border-gray-700">
      <pre>{JSON.stringify(resource, null, 2)}</pre>
    </div>
  );
};

// --- CONSOLIDATED FROM: ./stripe-nexus/NexusResourceJSON.tsx ---


import React from 'react';

export const NexusResourceJSON: React.FC<{ resource: object }> = ({ resource }) => {
  return (
    <div className="bg-gray-900 text-gray-100 p-4 rounded-md font-mono text-xs overflow-x-auto border border-gray-700">
      <pre>{JSON.stringify(resource, null, 2)}</pre>
    </div>
  );
};


// --- CONSOLIDATED FROM: ./stripe-nexus/NexusResourceJSON_2.tsx ---


import React from 'react';

export const NexusResourceJSON: React.FC<{ resource: object }> = ({ resource }) => {
  return (
    <div className="bg-gray-900 text-gray-100 p-4 rounded-md font-mono text-xs overflow-x-auto border border-gray-700">
      <pre>{JSON.stringify(resource, null, 2)}</pre>
    </div>
  );
};


// --- CONSOLIDATED FROM: ./stripe-nexus/NexusResourceJSON_1.tsx ---

import React from 'react';

export const NexusResourceJSON: React.FC<{ resource: object }> = ({ resource }) => {
  return (
    <div className="bg-gray-900 text-gray-100 p-4 rounded-md font-mono text-xs overflow-x-auto border border-gray-700">
      <pre>{JSON.stringify(resource, null, 2)}</pre>
    </div>
  );
};

// --- CONSOLIDATED FROM: ./components/stripe-nexus (1)/NexusResourceJSON.tsx ---

import React from 'react';

export const NexusResourceJSON: React.FC<{ resource: object }> = ({ resource }) => {
  return (
    <div className="bg-gray-900 text-gray-100 p-4 rounded-md font-mono text-xs overflow-x-auto border border-gray-700">
      <pre>{JSON.stringify(resource, null, 2)}</pre>
    </div>
  );
};

// --- CONSOLIDATED FROM: ./components/stripe-nexus (2)/NexusResourceJSON.tsx ---


import React from 'react';

export const NexusResourceJSON: React.FC<{ resource: object }> = ({ resource }) => {
  return (
    <div className="bg-gray-900 text-gray-100 p-4 rounded-md font-mono text-xs overflow-x-auto border border-gray-700">
      <pre>{JSON.stringify(resource, null, 2)}</pre>
    </div>
  );
};


// --- CONSOLIDATED FROM: ./components/stripe-nexus/NexusResourceJSON.tsx ---


import React from 'react';

export const NexusResourceJSON: React.FC<{ resource: object }> = ({ resource }) => {
  return (
    <div className="bg-gray-900 text-gray-100 p-4 rounded-md font-mono text-xs overflow-x-auto border border-gray-700">
      <pre>{JSON.stringify(resource, null, 2)}</pre>
    </div>
  );
};
