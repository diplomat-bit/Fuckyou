import React, { useContext, createContext } from 'react';
const Context = createContext({ data: { charge: [] } });
export const useStripeNexus = () => useContext(Context);

// --- CONSOLIDATED FROM: ./stripe-nexus/stripe-nexus-provider_1.tsx ---

import React, { useContext, createContext } from 'react';
const Context = createContext({ data: { charge: [] } });
export const useStripeNexus = () => useContext(Context);

// --- CONSOLIDATED FROM: ./stripe-nexus/stripe-nexus-provider_2.tsx ---


import React, { useContext, createContext } from 'react';
const Context = createContext({ data: { charge: [] } });
export const useStripeNexus = () => useContext(Context);
      

// --- CONSOLIDATED FROM: ./stripe-nexus/stripe-nexus-provider.tsx ---


import React, { useContext, createContext } from 'react';
const Context = createContext({ data: { charge: [] } });
export const useStripeNexus = () => useContext(Context);
      

// --- CONSOLIDATED FROM: ./components/stripe-nexus (1)/stripe-nexus-provider.tsx ---

import React, { useContext, createContext } from 'react';
const Context = createContext({ data: { charge: [] } });
export const useStripeNexus = () => useContext(Context);

// --- CONSOLIDATED FROM: ./components/stripe-nexus (2)/stripe-nexus-provider.tsx ---


import React, { useContext, createContext } from 'react';
const Context = createContext({ data: { charge: [] } });
export const useStripeNexus = () => useContext(Context);
      

// --- CONSOLIDATED FROM: ./components/stripe-nexus/stripe-nexus-provider.tsx ---


import React, { useContext, createContext } from 'react';
const Context = createContext({ data: { charge: [] } });
export const useStripeNexus = () => useContext(Context);
      